-- [[ BRAKYO SCRIPT HUB - FULL UPDATED VERSION ]]
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Player = Players.LocalPlayer

if CoreGui:FindFirstChild("BrakyoHub") then
    CoreGui.BrakyoHub:Destroy()
end

local BrakyoHub = Instance.new("ScreenGui")
BrakyoHub.Name = "BrakyoHub"
BrakyoHub.Parent = CoreGui
BrakyoHub.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local ValidKeys = {
    ["O5J6sql"] = true,
    ["6lhq071K"] = true,
    ["Y61Vql9d"] = true,
    ["Kr4i8P2k"] = true,
    ["Ob927pa"] = true
}

local FileName = "BrakyoHub_KeyData.json"
local KeyDuration = 12 * 60 * 60

local function CheckSavedKey()
    if not (readfile and writefile and isfile and delfile) then return false end
    if isfile(FileName) then
        local success, data = pcall(function()
            return game:GetService("HttpService"):JSONDecode(readfile(FileName))
        end)
        if success and data and data.ExpireTime then
            if os.time() < data.ExpireTime then
                return true
            else
                delfile(FileName)
            end
        end
    end
    return false
end

-- ==================== KEY SYSTEM FRAME ====================
local KeySystemFrame = Instance.new("Frame")
KeySystemFrame.Name = "KeySystemFrame"
KeySystemFrame.Parent = BrakyoHub
KeySystemFrame.BackgroundColor3 = Color3.fromRGB(0, 130, 150)
KeySystemFrame.BackgroundTransparency = 0.2
KeySystemFrame.BorderSizePixel = 0
KeySystemFrame.Position = UDim2.new(0.5, -200, 0.5, -110)
KeySystemFrame.Size = UDim2.new(0, 400, 0, 220)
KeySystemFrame.Draggable = true
KeySystemFrame.Active = true
KeySystemFrame.ClipsDescendants = true

local KeyCorner = Instance.new("UICorner")
KeyCorner.CornerRadius = UDim.new(0, 10)
KeyCorner.Parent = KeySystemFrame

local KeyStroke = Instance.new("UIStroke")
KeyStroke.Parent = KeySystemFrame
KeyStroke.Color = Color3.fromRGB(0, 255, 255)
KeyStroke.Thickness = 1.5

local KeyTitle = Instance.new("TextLabel")
KeyTitle.Parent = KeySystemFrame
KeyTitle.BackgroundTransparency = 1
KeyTitle.Position = UDim2.new(0, 0, 0, 15)
KeyTitle.Size = UDim2.new(1, 0, 0, 30)
KeyTitle.Font = Enum.Font.GothamBold
KeyTitle.Text = "⚡ BRAKYO HUB - KEY SYSTEM"
KeyTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyTitle.TextSize = 16

local KeyBox = Instance.new("TextBox")
KeyBox.Parent = KeySystemFrame
KeyBox.BackgroundColor3 = Color3.fromRGB(0, 100, 120)
KeyBox.BackgroundTransparency = 0.3
KeyBox.BorderSizePixel = 0
KeyBox.Position = UDim2.new(0.5, -170, 0, 60)
KeyBox.Size = UDim2.new(0, 340, 0, 38)
KeyBox.Font = Enum.Font.Gotham
KeyBox.PlaceholderText = "Nhập Key của bạn vào đây..."
KeyBox.Text = ""
KeyBox.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyBox.PlaceholderColor3 = Color3.fromRGB(200, 230, 240)
KeyBox.TextSize = 13
KeyBox.ClearTextOnFocus = false

local KeyBoxCorner = Instance.new("UICorner")
KeyBoxCorner.CornerRadius = UDim.new(0, 6)
KeyBoxCorner.Parent = KeyBox

local SubmitBtn = Instance.new("TextButton")
SubmitBtn.Parent = KeySystemFrame
SubmitBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 255)
SubmitBtn.Position = UDim2.new(0.5, -170, 0, 110)
SubmitBtn.Size = UDim2.new(0, 162, 0, 38)
SubmitBtn.Font = Enum.Font.GothamBold
SubmitBtn.Text = "XÁC NHẬN KEY"
SubmitBtn.TextColor3 = Color3.fromRGB(15, 15, 20)
SubmitBtn.TextSize = 13

local SubmitCorner = Instance.new("UICorner")
SubmitCorner.CornerRadius = UDim.new(0, 6)
SubmitCorner.Parent = SubmitBtn

local GetKeyBtn = Instance.new("TextButton")
GetKeyBtn.Parent = KeySystemFrame
GetKeyBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 120)
GetKeyBtn.BackgroundTransparency = 0.3
GetKeyBtn.Position = UDim2.new(0.5, 8, 0, 110)
GetKeyBtn.Size = UDim2.new(0, 162, 0, 38)
GetKeyBtn.Font = Enum.Font.GothamBold
GetKeyBtn.Text = "LẤY KEY"
GetKeyBtn.TextColor3 = Color3.fromRGB(230, 230, 230)
GetKeyBtn.TextSize = 13

local GetKeyCorner = Instance.new("UICorner")
GetKeyCorner.CornerRadius = UDim.new(0, 6)
GetKeyCorner.Parent = GetKeyBtn

local StatusLabel = Instance.new("TextLabel")
StatusLabel.Parent = KeySystemFrame
StatusLabel.BackgroundTransparency = 1
StatusLabel.Position = UDim2.new(0, 0, 0, 165)
StatusLabel.Size = UDim2.new(1, 0, 0, 25)
StatusLabel.Font = Enum.Font.GothamMedium
StatusLabel.Text = ""
StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
StatusLabel.TextSize = 12

GetKeyBtn.MouseButton1Click:Connect(function()
    local link = "https://link4sub.com/DLRKEw8ev5"
    if setclipboard then
        setclipboard(link)
        StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        StatusLabel.Text = "Đã sao chép link lấy key vào Clipboard!"
    else
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        StatusLabel.Text = "Link lấy key: " .. link
    end
end)

-- ==================== DISCLAIMER FRAME (MIỄN TRÁCH NHIỆM) ====================
local DisclaimerFrame = Instance.new("Frame")
DisclaimerFrame.Name = "DisclaimerFrame"
DisclaimerFrame.Parent = BrakyoHub
DisclaimerFrame.BackgroundColor3 = Color3.fromRGB(0, 130, 150)
DisclaimerFrame.BackgroundTransparency = 0.15
DisclaimerFrame.BorderSizePixel = 0
DisclaimerFrame.Position = UDim2.new(0.5, -200, 0.5, -110)
DisclaimerFrame.Size = UDim2.new(0, 400, 0, 220)
DisclaimerFrame.Visible = false
DisclaimerFrame.ZIndex = 20

local DiscCorner = Instance.new("UICorner")
DiscCorner.CornerRadius = UDim.new(0, 10)
DiscCorner.Parent = DisclaimerFrame

local DiscStroke = Instance.new("UIStroke")
DiscStroke.Parent = DisclaimerFrame
DiscStroke.Color = Color3.fromRGB(0, 255, 255)
DiscStroke.Thickness = 1.5

local DiscText = Instance.new("TextLabel")
DiscText.Parent = DisclaimerFrame
DiscText.BackgroundTransparency = 1
DiscText.Position = UDim2.new(0, 20, 0, 20)
DiscText.Size = UDim2.new(1, -40, 0, 110)
DiscText.Font = Enum.Font.GothamMedium
DiscText.Text = "⚠️ Miễn trách nhiệm vì đây là script trên mạng. Bạn có chắc chắn muốn tiếp tục sử dụng không?"
DiscText.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscText.TextSize = 14
DiscText.TextWrapped = true

local AcceptDiscBtn = Instance.new("TextButton")
AcceptDiscBtn.Parent = DisclaimerFrame
AcceptDiscBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 255)
AcceptDiscBtn.Position = UDim2.new(0.5, -75, 0, 145)
AcceptDiscBtn.Size = UDim2.new(0, 150, 0, 38)
AcceptDiscBtn.Font = Enum.Font.GothamBold
AcceptDiscBtn.Text = "XÁC NHẬN"
AcceptDiscBtn.TextColor3 = Color3.fromRGB(15, 15, 20)
AcceptDiscBtn.TextSize = 13

local AcceptDiscCorner = Instance.new("UICorner")
AcceptDiscCorner.CornerRadius = UDim.new(0, 6)
AcceptDiscCorner.Parent = AcceptDiscBtn

-- ==================== MIN BUTTON (ICON TRÒN) ====================
local MinButton = Instance.new("TextButton")
MinButton.Name = "MinButton"
MinButton.Parent = BrakyoHub
MinButton.BackgroundColor3 = Color3.fromRGB(0, 150, 170)
MinButton.BackgroundTransparency = 0.25
MinButton.BorderSizePixel = 0
MinButton.Position = UDim2.new(0.05, 0, 0.1, 0)
MinButton.Size = UDim2.new(0, 48, 0, 48)
MinButton.Font = Enum.Font.FredokaOne
MinButton.Text = "BK"
MinButton.TextColor3 = Color3.fromRGB(255, 255, 255)
MinButton.TextSize = 18
MinButton.Draggable = true
MinButton.Visible = false
MinButton.Active = true

local MinCorner = Instance.new("UICorner")
MinCorner.CornerRadius = UDim.new(1, 0)
MinCorner.Parent = MinButton

local MinStroke = Instance.new("UIStroke")
MinStroke.Parent = MinButton
MinStroke.Color = Color3.fromRGB(0, 255, 255)
MinStroke.Transparency = 0.2
MinStroke.Thickness = 2

-- ==================== MAIN FRAME ====================
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = BrakyoHub
MainFrame.BackgroundColor3 = Color3.fromRGB(0, 130, 150)
MainFrame.BackgroundTransparency = 0.2
MainFrame.BorderSizePixel = 0
MainFrame.Position = UDim2.new(0.5, -275, 0.5, -175)
MainFrame.Size = UDim2.new(0, 550, 0, 350)
MainFrame.Draggable = true
MainFrame.Active = true
MainFrame.ClipsDescendants = true
MainFrame.Visible = false

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Parent = MainFrame
MainStroke.Color = Color3.fromRGB(0, 255, 255)
MainStroke.Thickness = 1.5

-- ==================== CONFIRM CLOSE FRAME ====================
local ConfirmCloseFrame = Instance.new("Frame")
ConfirmCloseFrame.Name = "ConfirmCloseFrame"
ConfirmCloseFrame.Parent = MainFrame
ConfirmCloseFrame.BackgroundColor3 = Color3.fromRGB(0, 110, 130)
ConfirmCloseFrame.BackgroundTransparency = 0.1
ConfirmCloseFrame.Size = UDim2.new(1, 0, 1, 0)
ConfirmCloseFrame.Visible = false
ConfirmCloseFrame.ZIndex = 30

local ConfirmText = Instance.new("TextLabel")
ConfirmText.Parent = ConfirmCloseFrame
ConfirmText.BackgroundTransparency = 1
ConfirmText.Size = UDim2.new(1, 0, 0, 50)
ConfirmText.Position = UDim2.new(0, 0, 0.35, 0)
ConfirmText.Font = Enum.Font.GothamBold
ConfirmText.Text = "Bạn chắc chắn muốn đóng chứ?"
ConfirmText.TextColor3 = Color3.fromRGB(255, 255, 255)
ConfirmText.TextSize = 16
ConfirmText.ZIndex = 31

local YesBtn = Instance.new("TextButton")
YesBtn.Parent = ConfirmCloseFrame
YesBtn.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
YesBtn.Position = UDim2.new(0.5, -110, 0.55, 0)
YesBtn.Size = UDim2.new(0, 100, 0, 35)
YesBtn.Font = Enum.Font.GothamBold
YesBtn.Text = "Có"
YesBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
YesBtn.TextSize = 14
YesBtn.ZIndex = 31
Instance.new("UICorner", YesBtn).CornerRadius = UDim.new(0, 6)

local NoBtn = Instance.new("TextButton")
NoBtn.Parent = ConfirmCloseFrame
NoBtn.BackgroundColor3 = Color3.fromRGB(50, 70, 90)
NoBtn.Position = UDim2.new(0.5, 10, 0.55, 0)
NoBtn.Size = UDim2.new(0, 100, 0, 35)
NoBtn.Font = Enum.Font.GothamBold
NoBtn.Text = "Không"
NoBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
NoBtn.TextSize = 14
NoBtn.ZIndex = 31
Instance.new("UICorner", NoBtn).CornerRadius = UDim.new(0, 6)

-- ==================== TABS & CONTAINERS ====================
local RightContainer = Instance.new("Frame")
RightContainer.Name = "RightContainer"
RightContainer.Parent = MainFrame
RightContainer.BackgroundColor3 = Color3.fromRGB(0, 100, 120)
RightContainer.BackgroundTransparency = 0.3
RightContainer.BorderSizePixel = 0
RightContainer.Position = UDim2.new(0, 180, 0, 55)
RightContainer.Size = UDim2.new(0, 358, 0, 280)

local RightCorner = Instance.new("UICorner")
RightCorner.CornerRadius = UDim.new(0, 6)
RightCorner.Parent = RightContainer

local Pages = {}
local function CreateTab(name)
    local Page = Instance.new("ScrollingFrame")
    Page.Name = name .. "Page"
    Page.Parent = RightContainer
    Page.Active = true
    Page.BackgroundTransparency = 1
    Page.Size = UDim2.new(1, 0, 1, 0)
    Page.CanvasSize = UDim2.new(0, 0, 0, 0)
    Page.ScrollBarThickness = 4
    Page.Visible = false
    
    local Layout = Instance.new("UIListLayout")
    Layout.Parent = Page
    Layout.SortOrder = Enum.SortOrder.LayoutOrder
    Layout.Padding = UDim.new(0, 6)
    
    Pages[name] = Page
    return Page
end

local bfPage = CreateTab("Blox Fruit")
local saePage = CreateTab("Steal An Egg")

-- Check Saved Key on startup
if CheckSavedKey() then
    KeySystemFrame:Destroy()
    DisclaimerFrame:Destroy()
    MainFrame.Visible = true
    bfPage.Visible = true
end

SubmitBtn.MouseButton1Click:Connect(function()
    local enteredKey = KeyBox.Text
    if ValidKeys[enteredKey] then
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 255)
        StatusLabel.Text = "Xác nhận thành công!"
        
        if writefile then
            local expireTime = os.time() + KeyDuration
            local data = game:GetService("HttpService"):JSONEncode({ExpireTime = expireTime})
            writefile(FileName, data)
        end
        
        task.wait(0.5)
        KeySystemFrame:Destroy()
        DisclaimerFrame.Visible = true -- Hiện bảng miễn trách nhiệm
    else
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        StatusLabel.Text = "Key không chính xác hoặc đã hết hạn!"
    end
end)

AcceptDiscBtn.MouseButton1Click:Connect(function()
    DisclaimerFrame:Destroy()
    MainFrame.Visible = true
    bfPage.Visible = true
end)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Name = "TitleLabel"
TitleLabel.Parent = MainFrame
TitleLabel.BackgroundTransparency = 1
TitleLabel.Position = UDim2.new(0, 18, 0, 12)
TitleLabel.Size = UDim2.new(0, 150, 0, 30)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "⚡ BRAKYO HUB"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 15
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left

local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Name = "MinimizeBtn"
MinimizeBtn.Parent = MainFrame
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.Position = UDim2.new(1, -65, 0, 12)
MinimizeBtn.Size = UDim2.new(0, 25, 0, 25)
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = Color3.fromRGB(200, 230, 240)
MinimizeBtn.TextSize = 18

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Parent = MainFrame
CloseBtn.BackgroundTransparency = 1
CloseBtn.Position = UDim2.new(1, -35, 0, 12)
CloseBtn.Size = UDim2.new(0, 25, 0, 25)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 100, 100)
CloseBtn.TextSize = 14

CloseBtn.MouseButton1Click:Connect(function()
    ConfirmCloseFrame.Visible = true
end)

YesBtn.MouseButton1Click:Connect(function()
    local tween = TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)})
    tween:Play()
    tween.Completed:Wait()
    BrakyoHub:Destroy()
end)

NoBtn.MouseButton1Click:Connect(function()
    ConfirmCloseFrame.Visible = false
end)

local function animatePopup(show)
    if show then
        MinButton.Visible = false
        MainFrame.Visible = true
        MainFrame.Size = UDim2.new(0, 0, 0, 0)
        local tween = TweenService:Create(MainFrame, TweenInfo.new(0.25, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(0, 550, 0, 350)})
        tween:Play()
    else
        local tween = TweenService:Create(MainFrame, TweenInfo.new(0.2, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {Size = UDim2.new(0, 0, 0, 0)})
        tween:Play()
        tween.Completed:Wait()
        MainFrame.Visible = false
        MinButton.Visible = true
    end
end

MinimizeBtn.MouseButton1Click:Connect(function()
    animatePopup(false)
end)

MinButton.MouseButton1Click:Connect(function()
    animatePopup(true)
end)

local SearchBox = Instance.new("TextBox")
SearchBox.Name = "SearchBox"
SearchBox.Parent = MainFrame
SearchBox.BackgroundColor3 = Color3.fromRGB(0, 100, 120)
SearchBox.BackgroundTransparency = 0.3
SearchBox.BorderSizePixel = 0
SearchBox.Position = UDim2.new(0, 180, 0, 12)
SearchBox.Size = UDim2.new(0, 285, 0, 28)
SearchBox.Font = Enum.Font.Gotham
SearchBox.PlaceholderText = "Tìm kiếm script..."
SearchBox.Text = ""
SearchBox.TextColor3 = Color3.fromRGB(255, 255, 255)
SearchBox.PlaceholderColor3 = Color3.fromRGB(200, 230, 240)
SearchBox.TextSize = 13
SearchBox.ClearTextOnFocus = false

local SearchCorner = Instance.new("UICorner")
SearchCorner.CornerRadius = UDim.new(0, 6)
SearchCorner.Parent = SearchBox

local LeftMenu = Instance.new("ScrollingFrame")
LeftMenu.Name = "LeftMenu"
LeftMenu.Parent = MainFrame
LeftMenu.Active = true
LeftMenu.BackgroundTransparency = 1
LeftMenu.Position = UDim2.new(0, 12, 0, 55)
LeftMenu.Size = UDim2.new(0, 155, 0, 280)
LeftMenu.CanvasSize = UDim2.new(0, 0, 0, 0)
LeftMenu.ScrollBarThickness = 2

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Parent = LeftMenu
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 6)

local function AddScriptButton(targetPage, scriptName, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = targetPage
    btn.BackgroundColor3 = Color3.fromRGB(0, 100, 120)
    btn.BackgroundTransparency = 0.3
    btn.Size = UDim2.new(1, -10, 0, 36)
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = "  " .. scriptName
    btn.TextColor3 = Color3.fromRGB(230, 245, 255)
    btn.TextSize = 13
    btn.TextXAlignment = Enum.TextXAlignment.Left
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    
    btn.MouseButton1Click:Connect(function()
        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(0, 255, 255), TextColor3 = Color3.fromRGB(15, 15, 20)}):Play()
        task.wait(0.1)
        TweenService:Create(btn, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(0, 100, 120), TextColor3 = Color3.fromRGB(230, 245, 255)}):Play()
        pcall(callback)
    end)
    
    targetPage.CanvasSize = UDim2.new(0, 0, 0, targetPage.UIListLayout.AbsoluteContentSize.Y + 10)
end

SearchBox:GetPropertyChangedSignal("Text"):Connect(function()
    local text = SearchBox.Text:lower()
    for _, page in pairs(Pages) do
        for _, child in pairs(page:GetChildren()) do
            if child:IsA("TextButton") then
                if text == "" or child.Text:lower():find(text) then
                    child.Visible = true
                else
                    child.Visible = false
                end
            end
        end
    end
end)

local MenuButtons = {}

local function CreateMenuButton(name, targetPage)
    local btn = Instance.new("TextButton")
    btn.Parent = LeftMenu
    btn.BackgroundColor3 = Color3.fromRGB(0, 100, 120)
    btn.BackgroundTransparency = 0.4
    btn.Size = UDim2.new(1, -5, 0, 36)
    btn.Font = Enum.Font.GothamMedium
    btn.Text = "  " .. name
    btn.TextColor3 = Color3.fromRGB(200, 230, 240)
    btn.TextSize = 13
    btn.TextXAlignment = Enum.TextXAlignment.Left
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    
    table.insert(MenuButtons, {Button = btn, Page = targetPage})
    
    btn.MouseButton1Click:Connect(function()
        for _, p in pairs(Pages) do
            p.Visible = false
        end
        targetPage.Visible = true
        
        for _, item in ipairs(MenuButtons) do
            TweenService:Create(item.Button, TweenInfo.new(0.2), {
                BackgroundColor3 = Color3.fromRGB(0, 100, 120),
                TextColor3 = Color3.fromRGB(200, 230, 240),
                Size = UDim2.new(1, -5, 0, 36)
            }):Play()
        end
        
        TweenService:Create(btn, TweenInfo.new(0.15), {
            BackgroundColor3 = Color3.fromRGB(0, 160, 190),
            TextColor3 = Color3.fromRGB(255, 255, 255),
            Size = UDim2.new(1, -9, 0, 34)
        }):Play()
        
        task.wait(0.15)
        TweenService:Create(btn, TweenInfo.new(0.15), {
            Size = UDim2.new(1, -5, 0, 36)
        }):Play()
    end)
end

CreateMenuButton("Blox Fruit", bfPage)
CreateMenuButton("Steal An Egg", saePage)

AddScriptButton(saePage, "Night Hub V2", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/WhiteX1208/Scripts/refs/heads/main/StealAnEggs.luau"))()
end)

AddScriptButton(saePage, "Night Hub V1", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/WhiteX1208/Scripts/refs/heads/main/StealEggOnly.luau"))()
end)

AddScriptButton(saePage, "Sena", function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/senarblx/sena/refs/heads/main/loader'))()
end)

AddScriptButton(saePage, "LennonV2", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/lennonxscripts/lennonhubv2/main/stealaneggv2"))()
end)

AddScriptButton(bfPage, "RealKid", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/realkidhub/realkid/refs/heads/main/main.lua"))()
end)

AddScriptButton(saePage, "RealKid", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/realkidhub/realkid/refs/heads/main/main.lua"))()
end)
